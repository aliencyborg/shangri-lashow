<div id="music" class="flex flex-col w-full min-h-screen">
  <TopBar />
  {{#if this.isMobile}}
    <div class="flex flex-grow">
      <MusicPlayer />
    </div>
  {{else}}
    <div class="flex flex-grow bg-tape-player bg-no-repeat bg-contain bg-right">
      <div class="flex flex-col min-h-full">
        <MusicPlayer />
      </div>
    </div>
    <BottomBar />
  {{/if}}
</div>
{{#if this.isMobile}}
  <BottomBar />
{{/if}}