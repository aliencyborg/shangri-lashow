<div id="photos" class="">
  {{#photo-swipe classNames="bg-blackest" items=this.items history=false as |photoswipe|}}
    {{#each this.items as |item index|}}
      <img src={{item.src}} alt={{item.alt}} {{action photoswipe.actions.open (hash index=index)}} >
    {{/each}}
  {{/photo-swipe}}
</div>